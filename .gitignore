node_modules
dist
.env
.env.local
.dev.vars
.wrangler/
*.log
scripts/.entry-server.generated.mjs
*.tsbuildinfo

# Gerado por scripts/fetch-feeds.mjs e scripts/fetch-coupons.mjs a cada build —
# não versionado (muda toda semana e é grande demais pra valer a pena no git).
public/data/

# Config/estado local do Claude Code, não é parte do projeto.
.claude/

# Export bruto de transações da Awin (scripts/sanitize-sales-export.mjs) —
# dado comercial sensível (valor de venda, comissão, país do cliente, IDs de
# pagamento). Nunca deve ir pro repo, mesmo que baixado/colado aqui por engano.
/transa*.csv

# Vídeo diário gerado por scripts/generate-daily-video.mjs — artefato de
# build, não versionado (regenerado todo dia com dado fresco).
.video-tmp/
/daily-video.mp4
/daily-video.metadata.json
